# Public URL of the Flask API (Render). Required for production builds.
# Local `npm run dev` can leave this unset; Vite proxies /api to localhost:5000.
VITE_BACKEND_URL=https://hyperion-api-klr5.onrender.com
